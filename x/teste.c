//teste.c
#include "manipulador_de_lista_ligada.h"

int main()
{
	Lista_ligada *prog;

	inicializa_lista(prog);
	//void adiciona_linha(Linha_prog **programa, char **linha);
	//imprime_lista(prog);

	return 0;
}